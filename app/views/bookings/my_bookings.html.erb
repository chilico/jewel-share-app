<div class="container">
  <h3>This is all your bookings</h3>
  <div class="row justify-content-center">
    <% @my_bookings.each do |booking| %>
      <div class="card-product col-8 col-md-4 col-lg-3 m-3 px-4 rounded">
          <%= cl_image_tag booking.jewel.photos.first.key %>
        <div class="card-product-infos">
          <h2><%= booking.jewel.title %></h2>
          <p>£<%= booking.jewel.price %></p>
          <!--<p style="padding:0; margin:0">From <%= booking.start_date %></p>
          <p style="padding:0; margin:0">To <%= booking.end_date %></p> -->
        </div>
          <%= link_to ">", booking.jewel, class: "btn rounded-pill bottomright" %>
      </div>
    <% end %>
  </div>
</div>
